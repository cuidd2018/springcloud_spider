package mainentercore.spiderCore.spiderConfig.IConfig;

import mainentercore.spiderCore.spiderConfig.Configuration;

public interface CustomConfig {
    Configuration getConfig();
    void setConfig(Configuration config);
}
