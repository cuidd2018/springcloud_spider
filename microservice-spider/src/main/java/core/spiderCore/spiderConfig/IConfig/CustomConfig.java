package core.spiderCore.spiderConfig.IConfig;

import core.spiderCore.spiderConfig.Configuration;

public interface CustomConfig {
    Configuration getConfig();
    void setConfig(Configuration config);
}
